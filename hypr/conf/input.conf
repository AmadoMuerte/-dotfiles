# ============================================================
#  Input — keyboard and mouse
# ============================================================
input {
    # US + Russian layouts, switch with Alt+Shift
    kb_layout = us,ru
    kb_variant =
    kb_options = grp:alt_shift_toggle

    repeat_rate = 25
    repeat_delay = 300

    accel_profile = flat
}